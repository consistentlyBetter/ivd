% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_sensitivity.R
\name{.pip_reweight}
\alias{.pip_reweight}
\title{Analytic PIP re-weighting}
\usage{
.pip_reweight(pip, p0, p1)
}
\arguments{
\item{pip}{Numeric vector of PIPs estimated under \code{p0}.}

\item{p0}{Prior inclusion probability used in the fit.}

\item{p1}{New prior inclusion probability (scalar).}
}
\value{
Numeric vector of re-weighted PIPs.
}
\description{
Re-weight a posterior inclusion probability to a new prior
}
\details{
Converts a PIP estimated under prior inclusion probability \code{p0} into the
PIP implied by a different prior \code{p1}, without refitting: the Bayes factor
\verb{BF = [pip/(1-pip)] / [p0/(1-p0)]} does not depend on the prior, so the
re-weighted PIP is \code{BF*q / (1 + BF*q)} with \code{q = p1/(1-p1)}. PIPs of
exactly 0 or 1 are returned unchanged (they are invariant under any
\code{p1} in (0, 1)); see \code{\link[=pip_sensitivity]{pip_sensitivity()}} for the finite-sample clamp.
}
\keyword{internal}
