% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_sensitivity.R
\name{pip_sensitivity}
\alias{pip_sensitivity}
\title{Analytic prior-sensitivity for PIPs}
\usage{
pip_sensitivity(fit, prior_p = seq(0.05, 0.95, by = 0.05))
}
\arguments{
\item{fit}{A fitted \code{ivd} object.}

\item{prior_p}{Numeric vector of prior inclusion probabilities to
evaluate, all in (0, 1). Defaults to \code{seq(0.05, 0.95, by = 0.05)}.}
}
\value{
A data frame of class \code{pip_sensitivity} with one row per cluster
x scale random effect x \code{prior_p} value: \code{scale_var}, \code{cluster_index},
\code{cluster_id}, \code{prior_p}, \code{pip}, and \code{at_boundary}. The prior used in the
fit is stored in \code{attr(, "p0")}. Plot with \code{\link[=plot.pip_sensitivity]{plot.pip_sensitivity()}}.
}
\description{
Prior-sensitivity of the posterior inclusion probabilities
}
\details{
Shows how each cluster's PIP would change under different prior inclusion
probabilities \code{ss_prior_p}, computed analytically from the fitted model --
no refitting. The posterior odds are converted to a prior-independent
Bayes factor and re-weighted to each value of \code{prior_p}
(see \code{\link[=.pip_reweight]{.pip_reweight()}}).

PIPs estimated as exactly 0 or 1 are a finite-sample artifact (with \code{S}
post-warmup draws a PIP of 1 is only known to be at least \code{(S-0.5)/S}), so
they are clamped to \code{0.5/S} and \code{(S-0.5)/S} before re-weighting; the
\code{at_boundary} column flags those rows. All other PIPs are used as
estimated.
}
\examples{
\dontrun{
out <- ivd(y ~ 1 + (1 | school), ~ 1 + (1 | school), data, niter = 2000)
sens <- pip_sensitivity(out)
plot(sens)
}
}
\author{
Philippe Rast
}
