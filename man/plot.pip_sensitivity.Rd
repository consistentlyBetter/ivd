% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_sensitivity.R
\name{plot.pip_sensitivity}
\alias{plot.pip_sensitivity}
\title{Plot method for pip_sensitivity objects}
\usage{
\method{plot}{pip_sensitivity}(x, pip_level = 0.75, clusters = NULL, odds_factor = 2, ...)
}
\arguments{
\item{x}{A \code{pip_sensitivity} object from \code{\link[=pip_sensitivity]{pip_sensitivity()}}.}

\item{pip_level}{PIP threshold used to judge whether a cluster's
classification is prior-sensitive. Defaults to 0.75.}

\item{clusters}{Optional vector selecting which clusters to draw, matched
against \code{cluster_id} (the original grouping IDs) or \code{cluster_index}.
Defaults to \code{NULL} (all clusters).}

\item{odds_factor}{Width of the prior window used to judge sensitivity,
as a multiplicative factor on the fitted prior's odds. Defaults to 2.}

\item{...}{Not used.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot PIP trajectories across prior inclusion probabilities
}
\details{
One line per cluster (faceted by scale random effect when there is more
than one). By default all clusters are shown: those whose classification
at \code{pip_level} is prior-sensitive are drawn in color and labelled, robust
clusters are grey. With \code{clusters}, only the requested clusters are
drawn, each colored and labelled -- useful for inspecting individual
clusters or small groups. The dashed vertical line marks the prior used
in the fit.

As the prior inclusion probability approaches 0 or 1 every PIP follows
it, so crossing the threshold at extreme priors is expected and carries
no information. A cluster is therefore flagged as prior-sensitive only
when its classification changes within a \emph{plausible} perturbation of the
fitted prior: priors whose odds lie within a factor \code{odds_factor} of the
fitted prior's odds (the default, 2, spans halving to doubling them --
for \code{ss_prior_p = 0.5} that is priors between 1/3 and 2/3). Because the
re-weighted PIP is monotone in the prior, this is evaluated analytically
at the two ends of that window, independently of the plotted \code{prior_p}
grid.
}
\author{
Philippe Rast
}
