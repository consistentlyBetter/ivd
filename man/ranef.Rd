% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{ranef}
\alias{ranef}
\alias{ranef.ivd}
\title{Extract random effects from a fitted ivd model}
\usage{
ranef(object, ...)

\method{ranef}{ivd}(object, labels = c("index", "original"), ...)
}
\arguments{
\item{object}{An object of class \code{ivd}.}

\item{...}{Not used.}

\item{labels}{Row names: \code{"index"} (default) uses the internal 1..J
cluster index; \code{"original"} uses the user's original grouping IDs.}
}
\value{
Numeric matrix (J x (Kr + Sr)) of posterior means.
}
\description{
Extract random effects
}
\details{
Posterior means of the random effects \code{u}, one row per cluster and one
column per random effect. Location random effects come first, then the
scale random effects (prefixed with \code{scl_}). Note that the scale columns
report the unconditional \code{u}; multiply by the inclusion indicators (see
\code{\link[=pip]{pip()}}) to reproduce the model-implied, spike-and-slab-selected effect.

The generic is defined here so that \code{ivd} does not depend on \code{nlme}/\code{lme4};
if either package is attached, its same-named generic dispatches to this
method just the same.
}
\author{
Philippe Rast
}
