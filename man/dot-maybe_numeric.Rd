% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{.maybe_numeric}
\alias{.maybe_numeric}
\title{Parse cluster labels back to numeric when (and only when) they came from
a numeric grouping variable: a label is numeric-born iff converting and
re-formatting reproduces it exactly, so IDs like "007" or "A12" stay
character.}
\usage{
.maybe_numeric(labels)
}
\arguments{
\item{labels}{character vector of cluster labels}
}
\value{
Numeric vector when all labels round-trip, otherwise \code{labels}.
}
\description{
Parse cluster labels back to numeric when (and only when) they came from
a numeric grouping variable: a label is numeric-born iff converting and
re-formatting reproduces it exactly, so IDs like "007" or "A12" stay
character.
}
\keyword{internal}
