% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{.reconstruct_mu_means}
\alias{.reconstruct_mu_means}
\title{Reconstruct the posterior mean of the location predictor \code{mu}}
\usage{
.reconstruct_mu_means(obj)
}
\arguments{
\item{obj}{An \code{ivd} object (must carry the location design matrices \code{X}/\code{Z}).}
}
\value{
Numeric vector of length N: the posterior mean of \code{mu} per observation.
}
\description{
\code{mu} is no longer monitored (it stores O(N x iterations) values per chain).
Because \code{mu[i] = X[i, ] \%*\% beta + Z[i, ] \%*\% u[group_i, 1:Kr]} is \emph{linear}
in the monitored \code{beta} and \code{u}, the posterior mean of \code{mu} equals the
linear predictor evaluated at the posterior means of \code{beta} and \code{u} -- no
per-iteration \code{mu} storage required. Used by \code{plot.ivd()} for the cluster
outcome plot.
}
\keyword{internal}
